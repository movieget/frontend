import styled from 'styled-components'

const Header = () => {
  return <StyledHeader>무비겟</StyledHeader>
}

export default Header

const StyledHeader = styled.header`
  width: 100%;
  height: 74px;
  font-size: 4rem;
`
